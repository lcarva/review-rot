arguments:
  format: json
git_services:
- host: github.com
  repos:
  - hacbs-contract/chains
  - hacbs-contract/ec-policies
  - hacbs-contract/enterprise-contract-controller
  - redhat-appstudio/build-definitions
  - redhat-appstudio/e2e-tests
  - redhat-appstudio/infra-deployments
  token: "${GITHUB_TOKEN}"
  type: github
